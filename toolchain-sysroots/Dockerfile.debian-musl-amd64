# syntax=docker/dockerfile:1
# Minimal musl headers/libs on Debian amd64 for building GCC (same ABI as target sysroot).
# Matches sysroots/Dockerfile.debian-musl-amd64 — bookworm-slim with other musl arches.

FROM debian:bookworm-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    musl-dev \
    linux-libc-dev \
    && rm -rf /var/lib/apt/lists/*
